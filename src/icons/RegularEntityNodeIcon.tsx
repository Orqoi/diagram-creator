import React from 'react';

const RegularEntityNodeIcon = () => {
  return (
    <div
      style={{
        border: '1px solid black',
        background: '#fff',
        minHeight: 35, // Adjust height and width as per your icon size preference
        minWidth: 70,
        display: 'flex',
        justifyContent: 'center',
        alignItems: 'center',
      }}
    >
      {/* Add any design elements for your icon here */}
      {/* For example, you can add an SVG icon or any other design elements */}
      {/* <svg ...> ... </svg> */}
    </div>
  );
};

export default RegularEntityNodeIcon;
